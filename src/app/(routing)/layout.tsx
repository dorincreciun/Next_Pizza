import {CatalogLayoutProps} from "@/shared/types/layouts";

export default function CatalogLayout({ children }: CatalogLayoutProps){
    return (
        <>
            { children }
        </>
    )
}
